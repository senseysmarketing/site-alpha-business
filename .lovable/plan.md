

## Adicionar espaçamento entre os posts do Instagram

Ajuste em `src/components/InstitutionalSection.tsx`: trocar o `gap-[0.5px]` da grid por `gap-2` (8px) e remover o `bg-border/40` que servia como linha divisória entre os cards.


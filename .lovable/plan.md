

## Padronização de Fontes na Página de Detalhes

Ajustar todos os títulos em `PropertyDetail.tsx` para usar `text-display` (Raleway), seguindo o mesmo padrão da página inicial.

### Mudanças

**`src/pages/PropertyDetail.tsx`**:
- Linha 97: `h1` do título do imóvel — mudar `text-serif` para `text-display`
- Linha 102: Preço do imóvel — manter `text-serif` (preço pode usar serif para elegância) ou mudar para `text-display` se preferir consistência total
- Linha 142: "Sobre o Imóvel" — mudar `text-serif` para `text-display`
- Linha 152: "Detalhes do Imóvel" — mudar `text-serif` para `text-display`
- Linha 231: "Imóveis que você também pode gostar" — mudar `text-serif` para `text-display`
- Linha 252: Título dos cards similares — mudar `text-serif` para `text-display`

